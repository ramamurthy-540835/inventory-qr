export type BookingAct = 0 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8;
export type BookingState = { act: BookingAct; city: string | null; school: { id: string; name: string; address?: string; kitchenId?: string } | null; gradeBand: string | null; studentName: string; section: string; mealIds: string[]; allergyText: string; allergyAcknowledged: boolean; parentPhone: string; idempotencyKey: string | null };
export type BookingAction = { type: "OPEN" } | { type: "CITY"; city: string } | { type: "SCHOOL"; school: BookingState["school"] } | { type: "STUDENT"; studentName: string; section: string; gradeBand: string } | { type: "MEALS"; mealIds: string[] } | { type: "SAFETY"; allergyText: string; allergyAcknowledged: boolean } | { type: "PHONE"; parentPhone: string } | { type: "NEXT" } | { type: "BACK" } | { type: "BOOKING_KEY"; key: string } | { type: "CONFIRMED" };
export const initialBookingState: BookingState = { act: 0, city: null, school: null, gradeBand: null, studentName: "", section: "", mealIds: [], allergyText: "", allergyAcknowledged: false, parentPhone: "", idempotencyKey: null };
export function canEnterAct(state: BookingState, act: BookingAct) { if (act <= 0) return true; if (act >= 1 && !state.city) return false; if (act >= 2 && !state.school) return false; if (act >= 3 && (!state.gradeBand || state.studentName.trim().length < 2)) return false; if (act >= 4 && state.mealIds.length === 0) return false; if (act >= 6 && !state.allergyAcknowledged) return false; if (act >= 7 && !/^[6-9]\d{9}$/.test(state.parentPhone)) return false; return true; }
export function reducer(state: BookingState, action: BookingAction): BookingState { switch (action.type) { case "OPEN": return { ...state, act: 1 }; case "CITY": return { ...state, city: action.city, school: null, act: 2 }; case "SCHOOL": return { ...state, school: action.school, act: 3 }; case "STUDENT": return { ...state, studentName: action.studentName, section: action.section, gradeBand: action.gradeBand, act: 4 }; case "MEALS": return { ...state, mealIds: action.mealIds, act: 5 }; case "SAFETY": return { ...state, allergyText: action.allergyText, allergyAcknowledged: action.allergyAcknowledged, act: 7 }; case "PHONE": return { ...state, parentPhone: action.parentPhone }; case "NEXT": return { ...state, act: Math.min(8, state.act + 1) as BookingAct }; case "BACK": return { ...state, act: Math.max(0, state.act - 1) as BookingAct }; case "BOOKING_KEY": return state.idempotencyKey ? state : { ...state, idempotencyKey: action.key }; case "CONFIRMED": return { ...state, act: 8 }; default: return state; } }
